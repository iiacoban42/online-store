apiVersion: v1
kind: Service
metadata:
  name: stock-service-2
spec:
  type: ClusterIP
  selector:
    component: stock
  ports:
    - port: 7002
      name: http
      targetPort: 7002
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: stock
  template:
    metadata:
      labels:
        component: stock
    spec:
      containers:
        - name: stock
          image: bobdetest123/orderserivce:stock
          resources:
            limits:
              memory: "1Gi"
              cpu: "1"
            requests:
              memory: "1Gi"
              cpu: "1"
          command: ["gunicorn"]
          args: ["-b", "0.0.0.0:7002", "app:app"]
          ports:
            - containerPort: 7002
          env:
            - name: POSTGRES_HOST_0
              value: stock-db-0
            - name: POSTGRES_PORT_0
              value: '7101'
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: "test123"
            - name: POSTGRES_DB
              value: "0"
            - name: SERVICE_ID
              value: "0"
